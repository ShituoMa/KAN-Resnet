# KAN-Resnet
KAN-ResNet Model

For the implementation of KAN convolutional layer, reference code was provided https://github.com/AntonioTepsich/Convolutional-KANs Special thanks.

Add different type of pcaps into `./test_data/dataset/train` and `./test_data/dataset/test`, and run ./main.py, you can train your model.
